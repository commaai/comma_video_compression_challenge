# houdini

Same renderer family as quantizr / qpose14 / unified_brotli. Compresses the public dashcam clip via an AV1 mask (CRF=50, cpu-used=0), an FP4-quantized neural decoder (~88 K params), and a delta-encoded velocity side channel.
