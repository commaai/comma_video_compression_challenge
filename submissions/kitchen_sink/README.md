# kitchen_sink

CPU-evaluated submission. 178 KB archive: HNeRV decoder + 28-d per-frame-pair latents + per-pair single-dim latent sidecar perturbation chosen against PyAV-decoded ground truth.

Lineage: HNeRV decoder weights and architecture from PR #95 (hnerv_muon, AaronLeslie138).

Verified score on CPU+PyAV: 0.19797.
