# Attribution and scope

The `runtime/` and `cpr1/` source originated in [PR #135](https://github.com/commaai/comma_video_compression_challenge/pull/135), submission `semantic-pose-HPAC_CPR1_polished`, at commit `6dcf77164ccbdcc1e0e41c99312e65ace4bc1fb4`. The reference challenge checkout was `db52c5a9f05d5298e314fb0fa130290c4a350c4e`. `UPSTREAM_FILES.json` records SHA-256 hashes of the original copied files; `LICENSE.upstream` retains the repository's MIT notice.

The inherited [F26 release archive](https://github.com/codexblack/comma_video_compression_challenge/releases/download/semantic-pose-HPAC_CPR1_polished-f26/archive.zip) is 186,724 bytes, SHA-256 `12cf5d71a94065184f097c3e40dfe9f1db8402a1a76a80efc76a6956fe1e4004`. The learned semantic renderer, pose carrier, selector, integer HPAC model, residual table, and arithmetic token stream are inherited assets. They remain within the counted archive. No training or independent recreation of those assets is claimed.

Changes in this directory comprise a reversible storage representation for the inherited integer weight rows, its parser integration, archive repacking and verification, and an explicit MPS path that evaluates dyadic scale factors exactly on the CPU before copying them to MPS. CUDA remains the default and retains its existing power calculation. Full output parity and official evaluation are separate validation steps; successful byte reconstruction alone does not assert either result.

The new implementation, experiments, tests, and accompanying descriptions were generated by AI agents at the user's direction. The user has not claimed to have written or reviewed the code. This disclosure applies to new changes; attribution of inherited work remains with the upstream contributors.
