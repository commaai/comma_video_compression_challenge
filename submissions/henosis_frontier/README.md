# henosis_frontier

CUDA/GPU inflation is required.

Locally validated with Linux CUDA/DALI; rounded score is `0.30`.

The compressed artifact is hosted externally as `archive.zip` and is not committed to the repository.
